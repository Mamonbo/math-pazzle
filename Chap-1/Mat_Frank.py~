import fractions

TRIAL=100
Ainit=7

for idx in range(1,TRIAL):
    if idx==1:
        a=Ainit
        print('a(1) = {}'.format(a))
    else:
        a=a_prev+fractions.gcd(idx,a_prev)

        printmoji='a({0}) = {1} + gcd({gcd_a},{gcd_b}) = {1} + {gcd}'
    print(printmoji.format(0=idx,1=a_prev,gcd_a=idx))
